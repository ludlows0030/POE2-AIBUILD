# POE2 AI BD Agent

## 系统需求规格书

**Path of Exile 2 · 智能流派生成系统**

版本 1.0 | 2025

---

## 1. 项目概述

本系统旨在构建一个 AI Agent，能够自动采集 Path of Exile 2（POE2）社区中的真实玩家流派数据（BD），通过机器理解游戏机制，逻辑性地创造出新的、可行的 BD 方案，并向玩家提供带完整推理链的输出。

### 1.1 核心目标

- 从官方 API 和社区平台自动采集真实高端 BD 数据
- 解析并结构化存储技能、天赋、装备、词缀之间的机制关系
- 利用 LLM（大语言模型）推理引擎，基于逻辑组合生成全新 BD
- 向玩家输出附带推理说明的 BD 草案（技能链 + 天赋路径 + 装备建议）
- 系统持续自我更新以适应游戏版本迭代

### 1.2 成功指标

| 指标 | 目标值 | 验证方式 |
|------|--------|----------|
| BD 可行性通过率 | ≥ 70% | 资深玩家盲测评分 |
| 生成速度 | < 30 秒 | 端到端计时 |
| 数据覆盖率 | Top 1000 角色 | 官方 Ladder API |
| 机制理解准确率 | ≥ 85% | 规则引擎验证 |
| 版本更新响应时间 | < 48 小时 | Patch 发布后重采集 |

---

## 2. 系统架构总览

系统由五个核心模块构成，形成完整的数据流水线：

| 模块编号 | 模块名称 | 职责 |
|----------|----------|------|
| M1 | 数据采集 Agent | 从多渠道采集 BD 原始数据 |
| M2 | 数据解析与存储 | 结构化解析并持久化 |
| M3 | 机制知识图谱 | 构建技能/词缀/天赋关系图 |
| M4 | BD 推理引擎（LLM） | 逻辑组合生成新 BD |
| M5 | 输出与验证层 | 格式化输出并进行规则校验 |

### 2.1 数据流向

- 数据采集 Agent（M1）→ 原始数据库
- 解析器（M2）→ 结构化数据库（Postgres / MongoDB）
- 机制提取器 → 知识图谱（Neo4j / 向量数据库）
- 用户请求 → LLM 推理引擎（M4）← 知识图谱查询
- 推理结果 → 验证层（M5）→ 用户输出

---

## 3. 数据来源规格

### 3.1 官方 GGG API（主要来源）

GGG（Grinding Gear Games）提供官方开放 API，合法且免费，是最可靠的数据来源。

| API 端点 | 数据内容 | 采集频率 |
|----------|----------|----------|
| GET /ladders/{league} | 联赛排行榜 Top 1000 角色名 | 赛季开始 / 每周 |
| GET /character/{name}/items | 角色完整装备 + 词缀 | 每周更新 |
| GET /character/{name}/passives | 角色完整天赋树节点 | 每周更新 |
| GET /trade/data/items | 物品基础数据与词缀池 | 每次大版本后 |

**访问限制与合规要求：**

- 严格遵守 GGG API 速率限制（默认 45 req/min）
- 在 User-Agent 中标注项目名称与联系方式
- 不得商业出售 API 原始数据
- 不得采集私密角色（隐私设置为关闭的账户）

### 3.2 社区数据源

| 来源 | 数据类型 | 获取方式 | 合规性 |
|------|----------|----------|--------|
| PoEDB.com | 技能/词缀机制文本 | 公开 HTML 解析 | 遵守 robots.txt |
| pobb.in | 玩家上传 POB 链接 | API / RSS | 公开数据 |
| Path of Building | BD 完整导出格式 | 开源项目解析 | MIT 授权 |
| maxroll.gg | 攻略 BD 结构 | 人工整理 + 解析 | 需授权许可 |

### 3.3 数据结构定义

每个 BD 记录在数据库中应包含以下结构化字段：

- 技能组：主动技能 ID、支援宝石列表、触发条件
- 天赋树：已选节点 ID 列表、关键路径标注
- 装备词缀：各部位必须词缀 / 优先词缀 / 可选词缀
- 资源机制：魔力消耗模式、狂怒球来源、爆发条件
- 元数据：来源平台、采集时间、赛季版本、强度评级
- 机制标签：伤害类型、元素、攻击/法术、DOT/爆发

---

## 4. AI Agent 规格

### 4.1 Agent 架构

推荐采用 ReAct（Reasoning + Acting）架构，配合工具调用（Tool Use）模式：

| 工具名称 | 功能 | 输入 | 输出 |
|----------|------|------|------|
| query_builds_db | 查询历史 BD 数据库 | 流派类型、技能名 | BD 列表 + 评分 |
| get_skill_mechanics | 查询技能机制详情 | 技能 ID | 伤害公式、触发条件 |
| get_passive_graph | 查询天赋树相邻节点 | 起点节点 ID | 可达路径列表 |
| calculate_damage | 估算理论 DPS | 技能 + 词缀列表 | 伤害倍率估算值 |
| validate_build | 验证 BD 可行性 | BD 草案 JSON | 通过 / 错误报告 |
| search_synergies | 查找协同效应 | 技能 / 词缀关键词 | 协同关系列表 |

### 4.2 推理流程

Agent 接收用户请求后，按以下步骤推理生成 BD：

1. **Step 1 — 意图解析**：理解用户偏好（流派类型 / 预算 / 玩法风格）
2. **Step 2 — 参考检索**：从数据库拉取 3～5 个最相关的历史 BD 作为参考锚点
3. **Step 3 — 核心机制选择**：确定主输出技能及其核心机制
4. **Step 4 — 协同分析**：搜索与核心技能有乘算协同效应的天赋 / 词缀
5. **Step 5 — 资源验证**：确认魔力 / 生命 / 球类资源循环闭合
6. **Step 6 — 天赋路径规划**：在天赋图中寻找效率最优路径
7. **Step 7 — 装备需求推导**：基于技能需求反推关键词缀
8. **Step 8 — 规则校验**：调用 validate_build 工具检查合法性
9. **Step 9 — 输出生成**：附带每步推理说明，输出完整 BD 草案

### 4.3 LLM 选型建议

| 模型 | 优势 | 适用场景 |
|------|------|----------|
| Claude Sonnet（Anthropic） | 推理链清晰、Tool Use 稳定 | 主推理引擎 |
| GPT-4o（OpenAI） | 广泛支持、生态成熟 | 备选引擎 |
| 本地 Llama 3.1 70B | 数据不出境、低成本 | 批量数据预处理 |

### 4.4 Prompt 设计规范

系统 Prompt 必须包含以下内容：

- 游戏机制基础知识（乘算 vs 加算、暴击公式、元素穿透计算）
- 当前版本强度层级参考（Tier List）
- 禁止生成依赖已知 Bug / 被修复机制的 BD
- 输出格式规范（JSON 结构 + 人类可读说明）
- 推理透明度要求：每个关键选择必须给出理由

---

## 5. 功能需求

### 5.1 核心功能（MVP）

| 功能 ID | 功能名称 | 描述 | 优先级 |
|---------|----------|------|--------|
| F01 | BD 生成 | 根据用户输入生成完整 BD 草案 | P0 |
| F02 | BD 检索 | 从数据库搜索现有 BD | P0 |
| F03 | 数据采集 | 自动从 GGG API 采集排行榜数据 | P0 |
| F04 | 可行性验证 | 规则引擎检查 BD 合法性 | P0 |
| F05 | 版本更新 | Patch 后重新采集并标注失效 BD | P1 |

### 5.2 扩展功能（Phase 2）

- BD 对比分析：两个 BD 的 DPS / 生存 / 操作难度横向对比
- 成本优化：在固定预算内推荐最优装备替代方案
- 玩家上传：支持玩家上传自己的 BD 供 AI 分析改进
- 视频联动：解析 Twitch / YouTube 主播 BD 并结构化存储
- 多语言支持：中文、英文、繁体双语输出

---

## 6. 技术栈建议

| 层级 | 技术选型 | 用途 |
|------|----------|------|
| Agent 框架 | LangGraph / AutoGen | 多步骤 Agent 编排 |
| LLM 调用 | Anthropic SDK / OpenAI SDK | 推理引擎调用 |
| 关系数据库 | PostgreSQL | BD 结构化存储 |
| 图数据库 | Neo4j | 技能 / 天赋机制关系图 |
| 向量数据库 | Pinecone / Qdrant | BD 语义相似搜索 |
| 数据采集 | Python + httpx + BeautifulSoup | API 调用 + 网页解析 |
| 后端 API | FastAPI（Python） | Agent 服务对外接口 |
| 前端 | React + Tailwind CSS | 用户交互界面 |
| 部署 | Docker + AWS / GCP | 容器化部署 |
| 任务调度 | Celery + Redis | 定时采集 + 异步任务 |

---

## 7. 开发里程碑

| 阶段 | 时间 | 交付物 | 关键验收标准 |
|------|------|--------|--------------|
| Phase 0 基础建设 | Week 1-2 | GGG API 采集器 + 数据库 Schema | 成功采集 Top 500 角色数据 |
| Phase 1 MVP | Week 3-5 | 基础 BD 生成 Agent | 生成 10 个可行 BD，通过人工验证 |
| Phase 2 机制图谱 | Week 6-8 | 知识图谱 + 协同推理 | Agent 能识别 80% 以上已知协同 |
| Phase 3 产品化 | Week 9-12 | 完整前端 + API | 支持非技术用户使用，延迟 < 30s |
| Phase 4 持续运营 | 持续 | 版本监控 + 自动更新 | Patch 48h 内数据刷新 |

---

## 8. 风险评估与合规

### 8.1 主要风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| GGG 修改 API 政策 | 高 | 保持与官方条款同步，预留备用数据源 |
| 游戏版本大改动 | 高 | 模块化机制库，快速更新 + 版本标记 |
| AI 幻觉（输出不可行 BD） | 中 | 规则引擎强制验证 + 人工抽查 |
| 数据过时 | 中 | 每周自动采集 + 版本标注 |
| 第三方网站反爬 | 低 | 优先使用官方 API，减少爬虫依赖 |

### 8.2 合规声明

- 本系统仅使用 GGG 官方公开 API，不破解、不逆向工程客户端
- 采集社区数据时遵守目标网站 robots.txt 及使用条款
- 不出售或二次分发 API 原始数据
- AI 生成内容均标注为 AI 生成，不冒充玩家原创攻略
- 用户上传的 BD 数据须获得明确授权方可用于训练

---

## 9. 附录

### 9.1 输出格式示例

AI Agent 生成 BD 的标准输出 JSON 结构示意：

```json
{
  "build_name": "冰矛爆发法师",
  "version": "0.2.1d",
  "core_skill": "Ice Spear",
  "damage_type": ["Cold", "Projectile"],
  "reasoning": "选择冰矛是因为其二段爆炸机制与暴击率形成乘算...",
  "confidence": 0.82,
  "estimated_cost": "50-150 Divine"
}